import { css } from '@emotion/react';
import styled from '@emotion/styled';

export const Container = styled.div`
  ${() => css`
    padding: 50px 0;
    max-width: 1100px;
    margin: 0 auto;
  `}
`;
